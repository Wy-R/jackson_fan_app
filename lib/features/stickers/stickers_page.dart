import 'package:flutter/material.dart';

class StickersPage extends StatelessWidget {
  const StickersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('表情包')));
  }
}
